# Required
MONGO_URI=mongodb+srv://username:password@cluster.example.mongodb.net/supply_app
JWT_SECRET=replace-with-a-long-random-secret-at-least-16-chars

# Deployment platforms usually inject PORT automatically.
PORT=3001

# Optional MongoDB startup timeout, in milliseconds.
MONGO_SERVER_SELECTION_TIMEOUT_MS=15000
